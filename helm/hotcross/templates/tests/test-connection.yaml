apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hotcross.fullname" . }}-test-connection"
  labels:
    {{- include "hotcross.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hotcross.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
