apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cross-vesting-investor.fullname" . }}-test-connection"
  labels:
    {{- include "cross-vesting-investor.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cross-vesting-investor.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
